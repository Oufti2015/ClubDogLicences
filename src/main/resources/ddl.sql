drop table members;

create table members (
    member_id integer primary key,
    name text,
    first_name text,
    street text,
    num integer,
    post_code text,
    city text,
    phone text,
    gsm text,
    email text,
    birth_date text,
    country text,
    tongue text,
    dscp text,
    license text,
    account_id text,
    tech_id text,
    affiliated text,
    comite text
);